# apply-refactor-rules

Apply refactoring rules using opencode agent

## Parameters

--source-path (required), --rules-path (required), --dry-run (optional)

## Returns

Transformation report and diff

## Example

```bash
/apply-refactor-rules --help
```
