# run-benchmarks

Run benchmarks and compare to baseline

## Parameters

--benchmark-suite (required), --baseline (required)

## Returns

Performance comparison report

## Example

```bash
/run-benchmarks --help
```
