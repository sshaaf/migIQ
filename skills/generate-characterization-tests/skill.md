# generate-characterization-tests

Generate characterization tests using opencode agent

## Parameters

--source-path (required), --output (optional)

## Returns

Test suite with coverage report

## Example

```bash
/generate-characterization-tests --help
```
