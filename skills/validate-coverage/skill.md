# validate-coverage

Validate test coverage meets threshold

## Parameters

--coverage-report (required), --threshold (optional)

## Returns

Pass/Fail with coverage details

## Example

```bash
/validate-coverage --help
```
