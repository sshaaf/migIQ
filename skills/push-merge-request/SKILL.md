---
name: push-merge-request
description: Push and submit merge request to version control. Use when the user wants to submit a merge request, push code changes, create a pull request, or finalize code submission for review.
---

Push MR to CI platform

## Parameters

--mr-data (required), --platform (required)

## Returns

MR URL and pipeline ID

## Example

```bash
/push-merge-request --help
```
