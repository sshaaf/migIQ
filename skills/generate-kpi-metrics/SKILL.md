---
name: generate-kpi-metrics
trigger: /generate-kpi-metrics
description: Generate KPI tracking metrics for migration progress. Use when the user wants to track migration KPIs, measure progress metrics, monitor migration health, or report on migration status.
---

Generate KPI metrics using opencode agent

## Parameters

--time-range (optional), --output (optional)

## Returns

KPI dashboard data

## Example

```bash
/generate-kpi-metrics --help
```
